import javax.swing.*;
import java.awt.*;

// Press Shift twice to open the Search Everywhere dialog and type `show whitespaces`,
// then press Enter. You can now see whitespace characters in your code.
public class Main {
    public static void main(String[] args) {
        /*
        //jframe a gui window to add components to
        JFrame frame = new JFrame();    //creates a frame
        frame.setTitle("Program");//title of frame
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);   //exit out of application
        frame.setResizable(false);  //prevent frame from being resized
        frame.setSize(420, 420);    //set x-dim and y-dim
        frame.setVisible(true); //make frame visible (at this point you already have a tiny window)

        //ImageIcon imageIcon = new ImageIcon(url); create an imageIcon
        //frame.setIconImage(imageIcon.getImage()); change icon of frame
        frame.getContentPane().setBackground(Color.BLACK);//changes color of background
        //setBackground(new Color(r,g,b)
        */
        MyFrame myFrame = new MyFrame();
    }
}